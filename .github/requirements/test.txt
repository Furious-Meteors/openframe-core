# Test dependencies for openframe-core CI
# Install the package first: pip install -e .
# Then: pip install -r .github/requirements/test.txt

pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27

# Core runtime deps — pinned here to lock CI against minimum supported versions.
# The package's own pyproject.toml declares the same floors; these are explicit
# to catch regressions when upstream releases a new major.
pydantic>=2.0
pydantic-settings>=2.0
opentelemetry-api>=1.24
opentelemetry-sdk>=1.24
opentelemetry-exporter-otlp-proto-http>=1.24
opentelemetry-semantic-conventions>=0.45b0
opentelemetry-instrumentation-logging>=0.45b0
